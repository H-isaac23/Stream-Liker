import styles from "./ActiveStreams.module.scss";

const ActiveStreams = () => {
  return (
    <div>
      <div className={styles.container}>boiler plate</div>
    </div>
  );
};

export default ActiveStreams;
